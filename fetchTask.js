// ------ Heroku scheduled task to update the Etsy DBs every X hours.

//fetch everything first
//organize them into 'quiz pairs'
//then in a promise/callback, delete existing data (this shouldn't be an issue for people currently on the site as )
//then insert the fetch
